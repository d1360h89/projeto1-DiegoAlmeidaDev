Claro! Vou te mostrar alguns dos comandos principais do Git e como visualizar o histórico de commits. 😊

1. **Visualizando o histórico de commits:**

Git reflog
   Depois de criar vários commits ou clonar um repositório com histórico pré-existente, você provavelmente vai querer olhar para trás e ver o que aconteceu. A ferramenta mais básica e poderosa para isso é o comando `git log`. Por padrão, ele lista os commits em ordem cronológica inversa (o mais recente primeiro). Cada commit exibe seu checksum SHA-1, nome e email do autor, data de inserção e mensagem do commit. Veja um exemplo:

   ```
   $ git log
   commit ca82a6dff817ec66f44342007202690a93763949
   Author: Scott Chacon <schacon@gee-mail.com>
   Date: Mon Mar 17 21:52:11 2008 -0700
   changed the version number

   commit 085bb3bcb608e1e8451d4b2432f8ecbe6306e7e7
   Author: Scott Chacon <schacon@gee-mail.com>
   Date: Sat Mar 15 16:40:33 2008 -0700
   removed unnecessary test
   ```

   Você pode usar opções adicionais para personalizar a saída, como `-p` para mostrar as diferenças introduzidas em cada commit ou `-2` para listar apenas os dois últimos commits.

2. **Outros comandos essenciais:**
   - `git reset`: Reverte para um estado anterior do repositório.
   - `git tag`: Cria uma marca permanente no histórico.
   - `git rebase`: Reordena os commits no histórico.


nova branch
Git pull na master
Git checkout -b sistem-de-login master (cria a branch baseada na master)


Claro! Vou te mostrar como iniciar o Git e configurá-lo. 😊

1. **Instalação do Git:**
   Antes de começar, você precisa instalar o Git no seu sistema. Existem várias maneiras de fazer isso, dependendo do seu sistema operacional:
   - **Windows:** Você pode baixar o instalador do Git para Windows [aqui](https://gitforwindows.org/). Execute o instalador e siga as instruções.
   - **Linux (Ubuntu/Debian):** Abra o terminal e digite `sudo apt install git`.
   - **Mac:** O Git já está instalado por padrão no macOS.

2. **Configuração Inicial:**
   Após a instalação, é importante configurar algumas informações pessoais no Git:

   - **Nome de Usuário:**
     Defina seu nome de usuário globalmente para todos os projetos:
     ```
     $ git config --global user.name "Seu Nome"
     ```

   - **Endereço de Email:**
     Defina seu endereço de email globalmente:
     ```
     $ git config --global user.email "seu@email.com"
     ```

   Essas configurações são importantes porque cada commit que você fizer usará essas informações.

3. **Escolhendo um Editor de Texto:**
   O Git pode usar um editor de texto para inserir mensagens de commit. Por padrão, ele usa o Vim. Se você preferir outro editor (como o Nano ou o Emacs), configure-o assim:
   ```
   $ git config --global core.editor "nome_do_editor"
   ```

Agora você está pronto para começar a usar o Git! Se tiver mais alguma dúvida, é só perguntar. 😉


